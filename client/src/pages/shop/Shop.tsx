import React, { ReactElement } from 'react';

interface Props {}

function Shop({}: Props): ReactElement {
  return (
    <div>
      <h1>This is Shop page</h1>
    </div>
  );
}

export default Shop;
