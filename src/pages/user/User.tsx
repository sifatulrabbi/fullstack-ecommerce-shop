import React, { ReactElement } from 'react';

interface Props {}

function User({}: Props): ReactElement {
  return (
    <div>
      <h1>This is User page</h1>
    </div>
  );
}

export default User;
